<div class="container mt-2">
<%= simple_form_for [@restaurant, @review] do |f| %>
<%= f.input :content, as: :text  %>
<%= f.input :rating, as: :integer, error: "Pick a valid number from 0 to 5" %>
<%= f.button :submit %>
<% end %>
</div>
